[phases.setup]
nixPkgs = ["nodejs_20", "python312", "git"]

[phases.install]
cmds = [
  "npm install",
  "bash scripts/install-last30days.sh"
]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "tsx --tsconfig tsconfig.server.json server/index.ts"
