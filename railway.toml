[build]
builder = "NIXPACKS"

[deploy]
startCommand = "tsx --tsconfig tsconfig.server.json server/index.ts"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
healthcheckPath = "/api/health"
healthcheckTimeout = 60
